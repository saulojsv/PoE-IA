import { Bell, ChevronDown, Download, Menu, Search } from 'lucide-react'
import { useEffect, useState } from 'react'
export function TopHeader({onMenu,menuOpen}:{onMenu:()=>void;menuOpen:boolean}){const [open,setOpen]=useState(false); useEffect(()=>{const f=(e:KeyboardEvent)=>{if((e.metaKey||e.ctrlKey)&&e.key.toLowerCase()==='k'){e.preventDefault();setOpen(true)}};addEventListener('keydown',f);return()=>removeEventListener('keydown',f)},[]);return <header><button className="mobile-menu icon-btn" aria-label={menuOpen?'Close menu':'Open menu'} aria-expanded={menuOpen} onClick={onMenu}><Menu/></button><button className="searchbar" onClick={()=>setOpen(true)}><Search/><span>Search builds, skills, items...</span><kbd>⌘ K</kbd></button><div className="header-actions"><button className="select-btn">Necropolis League <ChevronDown/></button><button className="select-btn game">PoE 3.24 <ChevronDown/></button><button className="import"><Download/> <span>Import Build</span></button><button className="icon-btn"><Bell/></button><div className="avatar">C</div><div className="profile"><b>Cameron</b><small>Pro</small></div></div>{open&&<div className="command-backdrop" onMouseDown={()=>setOpen(false)}><div className="command" onMouseDown={e=>e.stopPropagation()}><div><Search/><input autoFocus placeholder="Search builds, skills, items..."/></div>{[['BUILDS','Lightning Arrow Deadeye','Ball Lightning Hierophant','Cyclone Slayer'],['SKILLS','Lightning Arrow','Elemental Hit','Toxic Rain'],['ITEMS','Blunderbore','The Brass Dome','Lightning Coil']].map(([g,...items])=><section key={g}><small>{g}</small>{items.map(x=><button key={x}>{x}</button>)}</section>)}</div></div>}</header>}
